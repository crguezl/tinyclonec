desc "Run tinyclone.rb with ruby"
task :tinyclone do
  sh "ruby -I. tinyclone.rb"
end

desc "Run tinyclone.rb with shotgun"
task :shotgun do
  sh "shotgun -I. tinyclone.rb"
end

desc "Remove the data base"
task :clean do
  sh "rm tinyclone.db"
end
